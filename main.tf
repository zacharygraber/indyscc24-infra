terraform {
  required_version = ">= 0.14.0"
  required_providers {
    openstack = {
      source  = "terraform-provider-openstack/openstack"
      version = "~> 1.53.0"
    }
  }
}

# Will grab credentials from env vars (e.g. sourced from an openrc file)
provider "openstack" { }